"""
Devin Optimized PDF Intelligence

A unified, optimized PDF search and query tool that combines the best ideas
from TF-IDF, BM25, FTS5, and evidence-based retrieval into a single
high-performance pipeline.

Key features:
- Multi-engine ranked fusion search (BM25 + TF-IDF + exact match)
- Concurrent PDF extraction with OCR fallback
- Smart snippet extraction with confidence scoring
- Document fingerprinting for deduplication
- CLI-first design with structured export (JSON, CSV, Markdown)
"""

__version__ = "1.0.0"
